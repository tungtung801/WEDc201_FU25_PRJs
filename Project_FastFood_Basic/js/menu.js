

document.addEventListener('DOMContentLoaded', function() {
    // tạo biến để lưu các thẻ <a>
    const navLinks = document.querySelector('#navBarFull li a');

    // forEach thực hiện hành động với mỗi phần tử
    navLinks.forEach(link => {
        
    });
});